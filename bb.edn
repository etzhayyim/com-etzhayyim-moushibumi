{:paths ["src" "test"]
 :tasks
 {test {:doc "Run moushibumi murakumo actor boundary tests"
        :task (do
                (require 'clojure.test
                         'moushibumi.murakumo-test)
                (let [run-tests (requiring-resolve 'clojure.test/run-tests)
                      r (run-tests 'moushibumi.murakumo-test)]
                  (System/exit (if (zero? (+ (:fail r) (:error r))) 0 1))))}}}
